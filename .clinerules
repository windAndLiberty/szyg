# 项目规则 (Cline Rules)

## 联网搜索 —— 优先使用 Tavily MCP

本项目已配置 **Tavily MCP** 服务,提供实时联网搜索能力(配置位于
`%APPDATA%/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`,
服务名 `tavily`)。它提供以下工具:

- `tavily_search` — 实时联网网页搜索
- `tavily_extract` — 提取指定 URL 的网页正文
- `tavily_map` — 生成网站结构地图
- `tavily_crawl` — 系统性爬取网站内容
- `tavily_research` — 多来源综合研究

### 触发条件

当遇到以下情况时,**必须优先调用 Tavily 工具联网获取最新信息,而不是凭记忆作答**:

1. 用户明确要求"搜索 / 查一下 / 查最新 / 联网"等
2. 涉及**时效性**信息:天气、新闻、股价、汇率、赛事比分、最新版本号、近期事件等
3. 信息可能**超出你的知识截止日期**(cutoff)
4. 你对某个事实**不确定**或**可能已过时**
5. 用户问"今天 / 最近 / 现在 / 2024年以后"等时间词相关的实时数据

### 使用原则

- 默认用 `tavily_search` 做通用搜索;需要深入某网页内容时再用 `tavily_extract`
- 需要广度研究(多来源汇总)时用 `tavily_research`
- 搜索后**如实引用来源 URL**,不要编造链接
- 若 Tavily 工具暂时不可用(MCP 未连接),应**明确告知用户当前无法联网**,而不是臆造答案
- 涉及安全/隐私敏感的本地文件操作不受此规则影响,仍按既定流程处理

### 一句话总结

> 只要任务需要"实时 / 最新 / 不确定"的外部信息,就调用 Tavily MCP 工具联网获取,绝不凭空捏造。
